// const numbers = []
// const limit = 10
// for (let index = 0; index < limit; index+=1) {
//     numbers.push(index)
// }
//Array.from({length:limit}).forEach((_, index) => {numbers.push(index)})
// const numbers = Array.from({length:limit}).map((_, index) => index)
//console.log(numbers);
// for (let index = 0; index < limit; index++) {
//     if (index % 2 == 0) {
//         numbers.push(index)
//     }
// }
// const pair = numbers.filter((itemCurrent) => itemCurrent % 2 == 0)
// console.log(pair);

// const numbersList = [2, 3, 4, 5, 6, 10, 9]
// const oddNumbers = numbersList.filter((CurrentItem) => CurrentItem % 2 == 0)
// const squareNumbers = oddNumbers.map((CurrentItem) => CurrentItem ** 2)
// console.log(squareNumbers);
